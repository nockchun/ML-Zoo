# 구현자용. 이 파일만 커밋한다. 실제 .env 는 커밋하지 않는다.
# 구현자 체크아웃: cp .env.example .env
# Makefile 이 이 값을 읽어 uvicorn·next·backend 에 넘긴다
FRONTEND_PORT=3000
BACKEND_PORT=8000
KIOSK_DB_PATH=data/kiosk.db
ADMIN_TOKEN=local-admin-token-change-me
