# 검증자 worktree 전용: cp .env.verify.example .env
# 구현자와 포트·DB 파일이 완전히 분리된다 (docs/environment.md 포트 할당표)
FRONTEND_PORT=3100
BACKEND_PORT=8100
KIOSK_DB_PATH=data/kiosk-verify.db
ADMIN_TOKEN=local-admin-token-change-me
